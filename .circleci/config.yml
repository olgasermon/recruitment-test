version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    context: test-context
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
            echo $MY_ENV_VAR
